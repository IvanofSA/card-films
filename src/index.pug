extends layout/template

block vars

block head
    //+loadCSS('/css/promo.css')

block content
    section.section
        .container
            .section__title.section__title_film Каталог фильмов
            .search
                i.search__icon
                input.search__input.js-search(type="text" autocomplete="off" name='search' placeholder="Введите название")
            .films
                ul.films__list.js-list-wrap
            button.js-btn.btn.btn_more Показать еще


block footer
    script(src="/js/main.bundle.js")